import { CheckboxColumnDirective } from './checkbox-column.directive';

describe('CheckboxColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxColumnDirective();
    expect(directive).toBeTruthy();
  });
});
